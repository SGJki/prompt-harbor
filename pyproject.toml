[project]
name = "prompt-harbor"
version = "0.1.0"
description = "Local transparent gateway for observing Codex and OpenAI API traffic"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
package = true

[project.scripts]
prompt-harbor = "prompt_harbor.core:main"

[tool.setuptools]
packages = ["prompt_harbor"]
